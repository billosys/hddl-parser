# Slice05: Corpus Test Routing

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| T5-1 | Slice05 starts from the committed Slice04 corpus policy base and preserves the corpus measurement command surface. | `git log --oneline --decorate -5`; `test -f tools/corpus_measure.rs`; `test -f tests/ipc/corpus-selections/fast.txt`; `rg -n "name = \"corpus_measure\"|path = \"tools/corpus_measure.rs\"" Cargo.toml`. | serious | Slice04 close | done | Local close started from `8e36a6a tools: add corpus selection policy`; CDC verified committed feature head `af0968b test: route corpus tests through fast selection` on top of that base; both policy files exist; `Cargo.toml` still declares `name = "corpus_measure"` and `path = "tools/corpus_measure.rs"`. | Expected base preserved. |
| T5-2 | The two inherited corpus ignored tests are enabled or replaced by enabled tests, leaving no Rust `#[ignore]` annotations in `src` or `tests`. | `rg -n "#\\[ignore" src tests -g '*.rs'`; inspect `tests/integration_ipc.rs` and `tests/integration_json.rs`. | serious | Arc05 goal | done | `rg -n "#\\[ignore" src tests -g '*.rs'` returned no matches; `tests/integration_ipc.rs::ipc_validation_test` and `tests/integration_json.rs::json_round_trip_ipc` now run without `#[ignore]`. | Replacement coverage is routed through `tests/common/mod.rs`. |
| T5-3 | Default IPC corpus coverage uses the checked-in fast selection and reports stable case IDs on failure. | `cargo test --locked --test ipc`; inspect the test/helper code for use of `tests/ipc/corpus-selections/fast.txt` or the shared fast-selection policy and case-ID diagnostics. | correctness | Slice04 policy | done | `cargo test --locked --test ipc` passed: 1 passed, 0 failed, 0 ignored, 0 filtered out, finished in 0.49s. `ipc_validation_test` iterates `common::fast_corpus_cases()` from `tests/ipc/corpus-selections/fast.txt` and includes `case.id` in read/parse/verify panic messages. | Default IPC test does not run all 900 cases. |
| T5-4 | Default JSON corpus coverage uses the checked-in fast selection and checks both structural and exact string round-trip equality. | `cargo test --locked --test json`; inspect the test/helper code for structural and exact string assertions over selected case IDs. | correctness | Slice03/Slice04 evidence | done | `cargo test --locked --test json` passed: 9 passed, 0 failed, 0 ignored, 0 filtered out, finished in 1.31s. `json_round_trip_ipc` iterates `common::fast_corpus_cases()`, parses/exports/reimports/verifies/re-exports, parses both outputs as `serde_json::Value`, and asserts exact plus structural equality with `case.id` in the failure message. | Slice03 found zero disagreements; both checks remain active in the default fast route. |
| T5-5 | Full corpus validation remains explicitly addressable and passes through the Slice04 policy command rather than an ignored test. | `HDDL_CORPUS_SELECTION=full HDDL_CORPUS_ASSERT=both cargo run --locked --example corpus_measure`; inspect docs for the full command. | serious | operator CI discussion | done | `HDDL_CORPUS_SELECTION=full HDDL_CORPUS_ASSERT=both cargo run --locked --example corpus_measure` passed from captured log: `selection=full, assertion=both, discovered_cases=900, selected_cases=900`; `summary attempted=900 completed=900 failures=0`; `json_equality_disagreements=0`; `json_assertion_failures=0`. | Full corpus remains an explicit command documented in `tests/ipc/corpus-selections/README.md`. |
| T5-6 | CI workflow policy remains unchanged by this routing slice. | `git diff -- .github/workflows`; `actionlint .github/workflows/ci.yml`. | serious | Slice05 boundary | done | `git diff -- .github/workflows` produced no output; `actionlint .github/workflows/ci.yml` passed. | CI branch/PR/main scheduling is the next slice. |
| T5-7 | Full local quality gates pass with no hidden skip debt. | `cargo fmt --check`; `cargo check --locked --all-targets`; `cargo test --locked --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --locked --all-targets`; `cargo clippy --locked --all-targets -- -D warnings`; `cargo build --release --locked --bins`; `./target/release/hddl_analyzer --help`; `git diff --check`; `git -C ../planning diff --check`. | serious | Arc05 gate | done | Passed: `cargo fmt --check`; `cargo check --locked --all-targets`; `cargo test --locked --all-targets` with lib 112 passed, `arc04_characterization` 11 passed, `current_behavior` 10 passed, `integration_flawed` 23 passed, IPC 1 passed/0 ignored, JSON 9 passed/0 ignored, LSP 6 passed; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --locked --all-targets`; `cargo clippy --locked --all-targets -- -D warnings`; `cargo build --release --locked --bins`; `./target/release/hddl_analyzer --help`; `git diff --check`; `git diff --cached --check`; `git -C ../planning diff --check`. | `cargo test --locked --all-targets` now runs the fast corpus routes with zero ignored tests. |
| T5-8 | Slice close bubbles the CI-policy boundary to Arc05 without silently wiring it. | Inspect `closing-report.md` for T5-1 through T5-8 and Bubble-up to Arc05. | correctness | project-management | done | `closing-report.md` added with T5-1 through T5-8 row walk and Arc05 bubble-up; CDC verification confirms workflows are unchanged and CI policy remains the next slice boundary. | Next slice should decide GitHub Actions branch-push, PR, scheduled, and post-merge/main corpus policy. |

## What Worked

- A small integration-test helper kept the fast corpus selection parsing,
  duplicate detection, file existence checks, and stable case IDs shared between
  the IPC and JSON routes without exposing new public crate API.
- The default tests now exercise representative corpus coverage while the full
  900-case run stays available through the existing Slice04 policy command.
- Capturing the full-corpus command output to `/private/tmp` preserved the
  command's final summary without losing the process status to terminal output
  truncation.

## Closure

CDC-verified on 2026-08-26. See `cdc-verification.md`.
