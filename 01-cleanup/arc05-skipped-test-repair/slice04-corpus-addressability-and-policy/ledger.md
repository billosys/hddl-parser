# Slice04: Corpus Addressability And Policy

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| CP5-1 | Slice04 starts from the committed Slice03 corpus measurement target and preserves the Cargo example invocation surface. | `git log --oneline --decorate -3`; `test -f tools/corpus_measure.rs`; `rg -n "name = \"corpus_measure\"|path = \"tools/corpus_measure.rs\"" Cargo.toml`; `HDDL_CORPUS_LIMIT=5 cargo run --locked --example corpus_measure`. | serious | Slice03 close | open | | Expected base is `c6e4907 tools: add corpus measurement utility`. |
| CP5-2 | A checked-in fast corpus selection policy exists, uses stable case IDs, is reviewable, has no duplicates, and resolves only to known corpus cases. | `test -f tests/ipc/corpus-selections/fast.txt`; `test -f tests/ipc/corpus-selections/README.md`; run the implemented duplicate/unknown validation path against `fast.txt`; inspect the selection rationale. | serious | Slice03 measurement | open | | Prefer plain text with comments over adding a parser dependency. |
| CP5-3 | The runner supports named `fast` and `full` selections plus a custom manifest path, with deterministic order and clear selection precedence. | `HDDL_CORPUS_SELECTION=fast HDDL_CORPUS_ASSERT=both cargo run --locked --example corpus_measure`; `HDDL_CORPUS_SELECTION=full HDDL_CORPUS_LIMIT=5 HDDL_CORPUS_ASSERT=both cargo run --locked --example corpus_measure`; `HDDL_CORPUS_MANIFEST=tests/ipc/corpus-selections/fast.txt HDDL_CORPUS_ASSERT=both cargo run --locked --example corpus_measure`; inspect docs for precedence. | correctness | corpus policy | open | | `HDDL_CORPUS_FILTER` and `HDDL_CORPUS_LIMIT` may remain iteration controls, but the policy commands should not depend on substring filters. |
| CP5-4 | Unknown case IDs, duplicate case IDs, invalid selection names, invalid manifest paths, and invalid assertion modes fail non-zero with actionable messages. | Create temporary bad selection files and run them through `HDDL_CORPUS_MANIFEST=... cargo run --locked --example corpus_measure`; run `HDDL_CORPUS_SELECTION=wat cargo run --locked --example corpus_measure`; run `HDDL_CORPUS_ASSERT=wat cargo run --locked --example corpus_measure`; record exit codes and stderr. | serious | robustness | open | | Do not panic for expected policy/input errors. |
| CP5-5 | JSON assertion policy is explicit and supports measurement-only, structural-only, exact-string-only, and both-equality validation. | Inspect `tools/corpus_measure.rs` for the assertion-mode enum/parser; run fast selection with `HDDL_CORPUS_ASSERT=none`, `structural`, `string`, and `both`; record all pass on the current corpus. | correctness | Slice03 equality evidence | open | | Recommended validation command uses `both`; measurement mode should still be possible. |
| CP5-6 | The fast selection is measurement-backed and avoids the Slice03 slow tail while preserving a representative spread of corpus domains. | Inspect `tests/ipc/corpus-selections/fast.txt` and `workbench/2026.08.26-corpus-measurement.md`; run the fast command and record selected case count, represented domain count, failures, equality disagreements, and slowest cases. | serious | operator policy discussion | open | | Do not choose cases by domain name alone; use Slice03 timing evidence. |
| CP5-7 | Default tests, inherited corpus ignored tests, and GitHub Actions remain unchanged by this policy-substrate slice. | `cargo test --locked --all-targets`; `rg -n "#\\[ignore" src tests -g '*.rs'`; `git diff -- .github/workflows tests/integration_ipc.rs tests/integration_json.rs`. | serious | arc-plan boundary | open | | The only allowed `Cargo.toml` change should be whatever is already present from Slice03. |
| CP5-8 | Full local quality gates pass and the slice close bubbles the next boundary to Arc05 without wiring CI. | `cargo fmt --check`; `cargo check --locked --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --locked --all-targets`; `cargo clippy --locked --all-targets -- -D warnings`; `cargo build --release --locked --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check`; inspect `closing-report.md` for CP5-1 through CP5-8 and Bubble-up. | serious | Arc05 gate | open | | The likely next slice is corpus test replacement/routing or CI wiring from the named policy commands. |

## What Worked

_(At slice close. Patterns that made the slice close cleanly.)_

## Closure

Open.
