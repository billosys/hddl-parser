# Slice02: Fast Ignored Test Repair

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F5-1 | `file_type_test` is rewritten as a meaningful enabled test that asserts parser classification for both a domain fixture and a problem fixture. | `cargo test --locked syntactic_analyzer::tests::syntax_tests::tests::file_type_test -- --exact` and inspect `src/syntactic_analyzer/tests/syntax_tests.rs` for absence of `#[ignore]` on `file_type_test` plus explicit `AbstractSyntaxTree::Domain` / `AbstractSyntaxTree::Problem` assertions. | correctness | Slice01 route matrix | done | Pending feature commit on `fix/fast-ignored-test-repair`: focused test passed with `1 passed; 0 failed; 0 ignored`; `file_type_test` has no `#[ignore]`, parses valid minimal domain/problem fixtures, and asserts `AbstractSyntaxTree::Domain` / `AbstractSyntaxTree::Problem` fields. | This is the one test-only fix from Slice01. |
| F5-2 | `forgotten_dash_validation_test` is repaired and enabled, with production-code changes limited to the malformed typed-parameter diagnostic path needed by the fixture. | `cargo test --locked --test flawed forgotten_dash_validation_test -- --exact` and inspect `tests/integration_flawed.rs` plus the touched parser files for a structured error assertion, not a broad success/failure check. | serious | Slice01 route matrix | done | Pending feature commit on `fix/fast-ignored-test-repair`: focused test passed with `1 passed; 0 failed; 0 ignored`; assertion pins syntactic `expected == "a variable name starting with '?'"`, `found == "Identifier airplane"`, line 33. Parser change adds strict declaration parsing for domain `:predicates` only. | Current behavior previously reported a later semantic arity error instead of the intended malformed-declaration diagnostic. |
| F5-3 | `forgotten_question_mark_validation_test` is repaired and enabled, with production-code changes limited to the malformed variable-name diagnostic path needed by the fixture. | `cargo test --locked --test flawed forgotten_question_mark_validation_test -- --exact` and inspect `tests/integration_flawed.rs` plus the touched parser files for a structured error assertion, not a broad success/failure check. | serious | Slice01 route matrix | done | Pending feature commit on `fix/fast-ignored-test-repair`: focused test passed with `1 passed; 0 failed; 0 ignored`; assertion pins syntactic `expected == "a variable name starting with '?'"`, `found == "Identifier s"`, line 35. Parser change rejects bare identifiers in domain predicate declarations before semantic verification. | Current behavior previously succeeded, so the test exposed missing validation. |
| F5-4 | The only remaining ignored Rust tests are the two corpus tests classified as slow/corpus-gate work. | `rg -n "#\\[ignore" src tests -g '*.rs'` reports only `tests/integration_ipc.rs` and `tests/integration_json.rs`, and neither corpus test body nor ignore annotation is modified by this slice. | correctness | operator-follow-up | done | Pending feature commit on `fix/fast-ignored-test-repair`: `rg -n "#\\[ignore" src tests -g '*.rs'` reports only `tests/integration_json.rs:17` and `tests/integration_ipc.rs:7`; `git diff -- tests/integration_ipc.rs tests/integration_json.rs` is empty. | Corpus execution policy remains separate; Slice03 planning is already present in the planning worktree. |
| F5-5 | The default locked local gate passes with the repaired tests enabled and without warning suppressions. | `cargo test --locked --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --locked --all-targets`; `cargo clippy --locked --all-targets -- -D warnings`; `cargo build --release --locked --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check`. | serious | Arc05 gate | done | Pending feature commit on `fix/fast-ignored-test-repair`: `cargo test --locked --all-targets` passed with lib `112 passed; 0 ignored`, flawed integration `23 passed`, IPC/JSON corpus tests still ignored; Rust 2024 compatibility check, clippy, release build, binary help smoke, actionlint, and feature `git diff --check` all passed. | No warning suppressions or assertion weakening were added. |
| F5-6 | Slice close evidence walks every row and bubbles remaining ignored-test work up to the arc without silent drops. | Inspect `closing-report.md` for F5-1 through F5-6 row walk and Bubble-up to the arc; `git -C ../planning diff --check`. | correctness | project-management | done | Pending planning commit: `closing-report.md` walks F5-1 through F5-6 and names the two remaining ignored tests as corpus-only; `git -C ../planning diff --check` passed. | The bubble-up preserves the slow/corpus measurement-first path and notes Slice03 planning is present without changing it. |

## What Worked

- Splitting domain predicate declarations from predicate atoms kept the new
  variable-name validation out of problem `:init` terms, where bare constants
  remain valid.
- Running `cargo test --locked --all-targets` before closing caught two semantic
  fixtures that encoded bare predicate declaration variables; adjusting those
  fixtures preserved their semantic duplicate/undefined-predicate oracles.

## Closure

Closed locally on 2026-08-26 against branch `fix/fast-ignored-test-repair`.
Feature commit: pending. Planning commit: pending. CDC verification: pending.
Total rows: 6. Done: 6. Deferred: 0. No-op: 0.
