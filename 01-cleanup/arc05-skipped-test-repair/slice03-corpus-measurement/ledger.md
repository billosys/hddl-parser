# Slice03: Corpus Measurement

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| M5-1 | A Rust-native corpus measurement utility is added under `examples/` and compiles as part of all-target checks without adding a new ignored test. | `test -f examples/corpus_measure.rs`; `cargo check --locked --all-targets`; `rg -n "corpus_measure" Cargo.toml examples tests`; `rg -n "#\\[ignore" tests src -g '*.rs'`. | serious | operator-follow-up | done | `examples/corpus_measure.rs` exists and `cargo check --locked --all-targets` passed. `rg -n "corpus_measure" Cargo.toml examples tests` finds only the example source comment. `rg -n "#\\[ignore" src tests -g '*.rs'` still reports only the two corpus ignores. | No manifest, workflow, or test harness was added. |
| M5-2 | Corpus enumeration is deterministic, sorted, and reports stable case IDs plus total domain/problem case count. | `HDDL_CORPUS_LIMIT=5 cargo run --locked --example corpus_measure` and inspect output/report for sorted case IDs and total discovered case count. | correctness | corpus discussion | done | Limit run passed: `discovered_cases=900, selected_cases=5`; first IDs were `AssemblyHierarchical/genericLinearProblem_depth01.hddl` through `AssemblyHierarchical/genericLinearProblem_depth05.hddl`; summary `attempted=5 completed=5 failures=0`. | Stable IDs use `<domain-dir>/<problem-file>`. |
| M5-3 | Parse/verify measurement records per-case timing and outcome without replacing the existing `ipc_validation_test` correctness gate. | `HDDL_CORPUS_REPORT=workbench/2026.08.26-corpus-measurement.csv cargo run --locked --example corpus_measure` and inspect the report for parse/verify fields and outcomes. | correctness | Slice01 route matrix | done | Full report run passed: `discovered_cases=900, selected_cases=900`, `attempted=900 completed=900 failures=0`. CSV has 901 lines including header and per-case `hddl_parse_ms`, `verify_ms`, `parse_outcome`, and `verify_outcome`. | `ipc_validation_test` remains ignored and unchanged for a later policy slice. |
| M5-4 | JSON round-trip measurement records phase-level timings per case: HDDL parse, JSON export, JSON import, reimport verification, JSON re-export, string equality, and structural JSON comparison. | Inspect the measurement report for all named phase columns and run at least one filtered measurement command that includes JSON timing. | serious | operator-follow-up | done | CSV header includes `hddl_parse_ms`, `verify_ms`, `json_export_ms`, `json_import_ms`, `reimport_verify_ms`, `json_reexport_ms`, `json_string_compare_ms`, and `json_value_compare_ms`. Blocksworld filtered run passed with `selected_cases=134`, `completed=134`, `failures=0`. | The full run shows JSON value comparison as the largest aggregate phase, 150003.572 ms / 36.35 percent of measured total. |
| M5-5 | Structural JSON comparison is measured separately from exact string equality so later tests can distinguish semantic round-trip failures from formatting/order-only mismatches. | Inspect measurement output/report for separate `json_string_equal` and `json_value_equal` or equivalent fields. | correctness | corpus discussion | done | CSV has separate `json_string_equal` and `json_value_equal` columns plus separate timing columns. All 900 completed cases had no JSON equality disagreement. | Existing JSON oracle behavior was not changed. |
| M5-6 | The workbench report summarizes full or bounded measurement evidence, including total cases attempted, completed cases, failures, slowest cases/phases, timeout/partial-run notes, and recommended next-slice options. | `test -f workbench/2026.08.26-corpus-measurement.md` and inspect summary sections; if a bounded run was needed, inspect the exact timeout command and partial output details. | serious | project-management | done | `workbench/2026.08.26-corpus-measurement.md` records the limit, Blocksworld, and full report runs. Full run completed normally with no external timeout: 900 attempted, 900 completed, 0 failures, 0 equality disagreements. | Workbench CSV/summary remain ignored evidence artifacts. |
| M5-7 | Default CI/test behavior is not changed by the measurement slice. | `cargo test --locked --all-targets`; `rg -n "#\\[ignore" src tests -g '*.rs'`; `git diff -- .github/workflows Cargo.toml tests/integration_ipc.rs tests/integration_json.rs`. | serious | arc-plan | done | `cargo test --locked --all-targets` passed. Ignore scan reports only `tests/integration_json.rs:17` and `tests/integration_ipc.rs:7`. Diff over `.github/workflows`, `Cargo.toml`, and the corpus integration tests is empty. | Corpus ignores remain for a later policy slice. |
| M5-8 | Full local quality checks pass and the slice close bubbles measurement findings up to Arc05 without choosing the final corpus policy. | `RUSTFLAGS="-D rust-2024-compatibility" cargo check --locked --all-targets`; `cargo clippy --locked --all-targets -- -D warnings`; `cargo build --release --locked --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check`; inspect `closing-report.md` for M5-1 through M5-8 and Bubble-up. | serious | Arc05 gate | done | Full local gate passed: fmt, locked check, locked tests, Rust 2024 compatibility check, clippy, release bin build, binary help, ignore scan, workflow lint, feature diff check, and planning diff check. `closing-report.md` records the row walk and bubble-up. | Recommended next boundary is corpus manifest/addressable-case/sample policy design, not final CI policy in this slice. |

## What Worked

- The Rust-native example finished the full 900-case IPC measurement run without
  an external timeout and without altering default tests or CI.
- Separate string and structural JSON equality measurement confirmed zero
  observed round-trip disagreements across the corpus.
- The data gives a concrete next-slice boundary: make the corpus addressable
  and policy-ready before deciding which subset belongs in branch CI.

## Closure

Slice03 is CDC-verified as of 2026-08-26.
