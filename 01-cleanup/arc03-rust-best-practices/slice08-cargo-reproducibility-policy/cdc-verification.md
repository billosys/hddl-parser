# CDC Verification: Arc03 Slice08 Cargo Reproducibility Policy

Date: 2026-08-26
Verifier: CDC
Feature branch: `fix/cargo-reproducibility-policy`
Feature base: `e14078c Fix LSP diagnostic lock scope`
Feature commit verified: `d820065 Fix Cargo reproducibility policy`
Planning branch: `planning`

## Verdict

Verified. Slice08 fixes RUST-006 by replacing wildcard dependency requirements
with explicit compatible semver requirements and by tracking Cargo's generated
lockfile for this binary-bearing package.

All C8 rows are verified done. No runtime behavior, source cleanup, LSP, CLI,
parser, transform, README, workflow, or public API change is mixed into the
feature diff.

## Artifact Boundary

`git diff --name-only e14078c..HEAD`:

```text
.gitignore
Cargo.lock
Cargo.toml
```

CDC inspected the feature diff. `.gitignore` keeps the existing broad `*.lock`
rule and adds `!Cargo.lock`; `Cargo.toml` replaces the three wildcard
requirements; `Cargo.lock` is generated by Cargo and begins with Cargo's
standard generated-file header.

## Policy Proof

Manifest requirements at `Cargo.toml:23-25`:

```toml
petgraph = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
```

Wildcard dependency grep:

```text
rg -n 'petgraph = "\*"|version = "1\.\*"|serde_json = "1\.\*"' Cargo.toml
```

The command exited `1` with no matches, which is the expected result for this
negative grep.

Lockfile tracking proof:

```text
git ls-files --error-unmatch Cargo.lock
```

The command exited `0` and printed `Cargo.lock`.

CDC also ran `cargo check --locked --all-targets` as an additional
reproducibility check. It exited `0`, confirming the tracked lockfile can be
used without dependency-resolution drift.

## Quality Gate

CDC reproduced the Slice08 gate:

- `cargo fmt --check`
- `cargo check --all-targets`
- `cargo check --locked --all-targets`
- `RUSTFLAGS="-D rust-2024-compatibility" cargo check --all-targets`
- `cargo clippy --all-targets -- -D warnings`
- `cargo test --all-targets`
- `cargo build --release --bins`
- `./target/release/hddl_analyzer --help`
- `actionlint .github/workflows/ci.yml`
- `git diff --check`
- `git diff --cached --check`

All commands exited `0`, except the negative wildcard `rg`, which correctly
exited `1` with no matches.

`cargo test --all-targets` passed with the existing ignored legacy tests
unchanged:

- library tests: 111 passed, 1 ignored
- `current_behavior`: 10 passed
- `integration_flawed`: 21 passed, 2 ignored
- `integration_ipc`: 1 ignored
- `integration_json`: 8 passed, 1 ignored
- `lsp_current_behavior`: 6 passed

## Ledger Walk

- C8-1: Verified. Feature commit `d820065` changes only `.gitignore`,
  `Cargo.lock`, and `Cargo.toml` relative to `e14078c`.
- C8-2: Verified. Wildcard dependency requirements are removed and replaced
  with explicit compatible semver requirements.
- C8-3: Verified. `Cargo.lock` is tracked, and `.gitignore` explicitly exempts
  it from the existing `*.lock` rule.
- C8-4: Verified. Cargo-generated dependency resolution validates through
  `cargo check --all-targets` and `cargo check --locked --all-targets`.
- C8-5: Verified. No runtime, source, test, workflow, README, CLI, LSP,
  parser, transform, or public API change is mixed in.
- C8-6: Verified. Full local workflow-equivalent gate passed.
- C8-7: Verified. Closing report walks every row and states the final
  dependency and lockfile policy.

## Bubble-Up

Slice08 delivers the final production repair slice opened by Slice03. Arc03 no
longer needs additional repair-slice planning before arc-level closure. RUST-007
remains intentionally deferred to Arc04 public API cohesion, and Slice07 C7-5
retains its specific runtime-contention test re-entry condition.
