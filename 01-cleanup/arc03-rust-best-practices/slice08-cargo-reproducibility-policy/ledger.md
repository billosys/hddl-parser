# Slice08: Cargo Reproducibility Policy Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| C8-1 | The slice diff is limited to Cargo reproducibility policy and generated lockfile state, on top of the verified Slice07 base. | `git diff --name-only e14078c..HEAD` unless Slice07 has merged upstream, then inspect changed files. | serious | RUST-006 | done | Feature commit `d820065`; `git diff --name-only e14078c..HEAD` -> `.gitignore`, `Cargo.lock`, `Cargo.toml`. | Expected files only: manifest requirements, lockfile ignore exception, and generated lockfile. |
| C8-2 | Wildcard dependency requirements are replaced with explicit compatible semver requirements. | `rg -n 'petgraph = "\\*"|version = "1\\.\\*"|serde_json = "1\\.\\*"' Cargo.toml` expecting no matches, plus inspect replacements. | serious | RUST-006 | done | `rg -n 'petgraph = "\\*"|version = "1\\.\\*"|serde_json = "1\\.\\*"' Cargo.toml` -> no matches; `Cargo.toml:23-25` now uses `petgraph = "0.8"`, `serde = { version = "1.0", features = ["derive"] }`, and `serde_json = "1.0"`. | These are compatible semver requirements matching the generated lockfile's major-compatible resolved packages. |
| C8-3 | Lockfile policy is resolved for this package with binaries. | `git ls-files Cargo.lock`; inspect `.gitignore`; inspect closing-report rationale. | serious | RUST-006 | done | `git ls-files Cargo.lock` -> `Cargo.lock`; `.gitignore:4-5` keeps `*.lock` but adds `!Cargo.lock`; closing report records binary-package policy. | Policy: track `Cargo.lock` for reproducible binary builds. |
| C8-4 | Cargo regenerates or validates dependency resolution without manual lockfile edits. | `cargo check --all-targets` after lockfile/policy changes. | correctness | cargo-guidelines | done | `cargo generate-lockfile` succeeded after network escalation; CDC reproduced `cargo check --all-targets` and `cargo check --locked --all-targets`, both exit `0`. | Initial sandboxed `cargo generate-lockfile` failed on DNS; no manual `Cargo.lock` content edits were made. |
| C8-5 | No runtime behavior, source cleanup, LSP, CLI, or public API change is mixed in. | `git diff --name-only e14078c..HEAD` unless Slice07 has merged upstream, then inspect non-Cargo files. | correctness | slice03-fix-map | done | Feature commit `d820065`; `git diff --name-only e14078c..HEAD` -> `.gitignore`, `Cargo.lock`, `Cargo.toml`; no source, test, workflow, README, CLI, LSP, parser, transform, or public API files changed. | Cargo-policy-only feature diff. |
| C8-6 | The full local workflow-equivalent gate passes. | `cargo fmt --check`; `cargo check --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test --all-targets`; `cargo build --release --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check` | serious | arc-plan | done | Feature commit `d820065`; CDC reproduced all requested commands with exit `0`, plus `cargo check --locked --all-targets`. | `cargo test --all-targets` reported lib 111 passed/1 ignored; current behavior 10 passed; flawed 21 passed/2 ignored; ipc 1 ignored; json 8 passed/1 ignored; LSP 6 passed. |
| C8-7 | Slice08 closing report walks every row and states the final dependency/lockfile policy. | Inspect `closing-report.md` for C8-1 through C8-7. | correctness | ledger-discipline | done | `closing-report.md` added with C8-1 through C8-7 walk, final manifest requirements, lockfile policy, and Cargo-generation statement; CDC added `cdc-verification.md`. | Feature commit `d820065`. |

## What Worked

The broad `*.lock` ignore rule was the only obstacle to tracking `Cargo.lock`; adding `!Cargo.lock` kept the existing ignore policy intact while making the binary package's lockfile observable to Git. Using Cargo's generated lockfile plus full gate verification kept the slice behavior-neutral.

## Closure

Slice08 is closed locally with all C8 rows done at feature commit `d820065`. The final policy is to use explicit compatible dependency requirements in `Cargo.toml` and track Cargo's generated `Cargo.lock` for this binary-bearing package. No runtime, LSP, CLI, parser, transform, README, workflow, or public API files changed.
