# Arc03 Slice08 Closing Report: Cargo Reproducibility Policy

Date: 2026-08-26
Feature branch: `fix/cargo-reproducibility-policy`
Base commit: `e14078c Fix LSP diagnostic lock scope`
Feature commit: `d820065 Fix Cargo reproducibility policy`

## Summary

Slice08 fixes RUST-006 by replacing wildcard dependency requirements with explicit compatible semver requirements and by tracking Cargo's generated lockfile for this binary-bearing package.

Feature files:

- `.gitignore`
- `Cargo.toml`
- `Cargo.lock`

No runtime behavior, LSP, CLI, parser, transform, README, workflow, or public API files changed.

## Final Policy

Manifest requirements:

- `petgraph = "0.8"`
- `serde = { version = "1.0", features = ["derive"] }`
- `serde_json = "1.0"`

Lockfile policy:

- Track `Cargo.lock` because this package ships binaries.
- Keep the existing broad `*.lock` ignore rule.
- Add `!Cargo.lock` so the Cargo lockfile is explicitly included.

`Cargo.lock` was generated by Cargo with `cargo generate-lockfile`; its contents were not manually edited. The first sandboxed attempt failed because crates.io DNS was unavailable, then the same command succeeded with network escalation.

## Verification

Commands run locally:

- `rg -n 'petgraph = "\*"|version = "1\.\*"|serde_json = "1\.\*"' Cargo.toml` -> no matches
- `git ls-files Cargo.lock` -> `Cargo.lock`
- `cargo generate-lockfile` -> passed after network escalation
- `cargo fmt --check` -> passed
- `cargo check --all-targets` -> passed
- `cargo check --locked --all-targets` -> passed in CDC verification
- `RUSTFLAGS="-D rust-2024-compatibility" cargo check --all-targets` -> passed
- `cargo clippy --all-targets -- -D warnings` -> passed
- `cargo test --all-targets` -> passed
- `cargo build --release --bins` -> passed
- `./target/release/hddl_analyzer --help` -> passed
- `actionlint .github/workflows/ci.yml` -> passed
- `git diff --check` -> passed

Scope checks:

- `git rev-parse --short HEAD` -> `d820065`
- `git diff --name-only e14078c..HEAD` -> `.gitignore`, `Cargo.lock`, `Cargo.toml`

## Ledger Walk

- C8-1: Done. Feature diff is limited to Cargo reproducibility policy and generated lockfile state on base `e14078c`.
- C8-2: Done. Wildcard dependency requirements were replaced with explicit compatible semver requirements.
- C8-3: Done. `Cargo.lock` is tracked in the index, and `.gitignore` explicitly exempts it from `*.lock`.
- C8-4: Done. Cargo generated the lockfile and `cargo check --all-targets` plus `cargo check --locked --all-targets` validate the resulting dependency resolution.
- C8-5: Done. No runtime behavior, source cleanup, LSP, CLI, parser, transform, README, workflow, or public API change is mixed in.
- C8-6: Done. The full local workflow-equivalent gate passes.
- C8-7: Done. This report walks every row and states the final manifest and lockfile policy.
