# Slice05: Structured Parser And Transform Errors Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| C5-1 | The slice diff is limited to parser/transpiler/transform error handling and directly updated baselines, on top of the verified Slice04 CLI base. | `git diff --name-only f3a3f8d..HEAD` unless Slice04 has merged upstream, then inspect changed files. | serious | RUST-002/RUST-003 | done | Feature commit `6bd1b0a` changes `src/lib.rs`, `src/transpiler/transformations/remove_equality.rs`, and `tests/current_behavior.rs` relative to `f3a3f8d`. | Scope is limited to RUST-002/RUST-003 parser/transpiler/transform error handling and updated baselines. |
| C5-2 | `HDDLProgram::from_hddl` returns `Err` for problem-as-domain input instead of panicking. | Updated `tests/current_behavior.rs` test plus focused `cargo test --test current_behavior`. | serious | RUST-002 | done | Feature commit `6bd1b0a`; `hddl_program_problem_as_domain_returns_error` passed in CDC's `cargo test --test current_behavior` run with 10 passed, 0 failed. | Error text: `expected domain input, found problem`. |
| C5-3 | `HDDLProgram::from_hddl` returns `Err` for domain-as-problem input instead of panicking. | Updated `tests/current_behavior.rs` test plus focused `cargo test --test current_behavior`. | serious | RUST-002 | done | Feature commit `6bd1b0a`; `hddl_program_domain_as_problem_returns_error` passed in CDC's `cargo test --test current_behavior` run with 10 passed, 0 failed. | Error text: `expected problem input, found domain`. |
| C5-4 | `Transpiler::from_hddl` returns `Err` for both domain/problem mismatch directions instead of panicking. | Updated `tests/current_behavior.rs` tests plus focused `cargo test --test current_behavior`. | serious | RUST-002 | done | Feature commit `6bd1b0a`; `transpiler_domain_as_problem_returns_error` and `transpiler_problem_as_domain_returns_error` passed in CDC's `cargo test --test current_behavior` run with 10 passed, 0 failed. | `Transpiler::from_hddl` propagates the `HDDLProgram::from_hddl` `ParsingError`. |
| C5-5 | Domain-only `RemoveEqualityConstraints` returns `Err` before mutation and does not panic. | Updated `tests/current_behavior.rs` transform test plus focused `cargo test --test current_behavior`. | serious | RUST-003 | done | Feature commit `6bd1b0a`; `remove_equality_constraints_domain_only_returns_error` passed in CDC's `cargo test --test current_behavior` run with 10 passed, 0 failed. | The `Result`-returning method checks for `problem` before calling the mutating helper. |
| C5-6 | Public error messages are specific enough to distinguish unexpected domain/problem variants and missing problem input. | Inspect updated `ParsingError` or equivalent error construction and test assertions. | correctness | rust-guidelines | done | Feature commit `6bd1b0a`; code inspection shows `ParsingError::Transformation("expected domain input, found problem")`, `ParsingError::Transformation("expected problem input, found domain")`, and `ParsingError::Transformation("remove-equality-constraints requires a problem input")`; tests assert the distinguishing substrings. | Reused the existing `ParsingError::Transformation` channel per slice constraint. |
| C5-7 | The two Slice01 panic runtime probes no longer exit with panic code `101`. | Rerun the problem-as-domain and domain-only remove-equality runtime probes from the audit report. | serious | slice01-audit | done | Feature commit `6bd1b0a`; CDC reproduced both probes: problem-as-domain exited `1` with `[Error] expected domain input, found problem`; domain-only remove-equality exited `1` with `[Error] remove-equality-constraints requires a problem input`. | Depends on Slice04 CLI error-exit behavior for status `1`. |
| C5-8 | The full local workflow-equivalent gate passes. | `cargo fmt --check`; `cargo check --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test --all-targets`; `cargo build --release --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check` | serious | arc-plan | done | Feature commit `6bd1b0a`; CDC reproduced all listed commands with exit `0` on 2026-08-26. Focused `cargo test --test current_behavior` also exited `0` with 10 passed, 0 failed. | `cargo test --all-targets` passed with the existing ignored legacy tests unchanged. |
| C5-9 | Slice05 closing report walks every row and states the final RUST-002/RUST-003 error contracts. | Inspect `closing-report.md` for C5-1 through C5-9. | correctness | ledger-discipline | done | Feature commit `6bd1b0a`; `closing-report.md` records C5-1 through C5-9, final public error behavior, and the Slice04 CLI dependency. | |

## What Worked

The Slice02 characterization tests made the behavior change narrow: every
former `catch_unwind` baseline became an ordinary `Err` assertion against the
same public surface. Reusing `ParsingError::Transformation` avoided a public
API expansion while still giving callers distinguishable failure messages.

## Closure

Closed by CC on 2026-08-25 as feature commit `6bd1b0a` on
`fix/structured-parser-transform-errors`. Final row count: C5-1 through C5-9
done, with no deferred or no-op Slice05 rows.
