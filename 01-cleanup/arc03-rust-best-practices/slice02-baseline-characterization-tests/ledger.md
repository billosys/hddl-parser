# Slice02: Baseline Characterization Tests Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| C3-1 | The slice diff is test-only and contains no production behavior changes. | `git diff --name-only main..HEAD` and inspect any `src/` edits for `#[cfg(test)]`-only additions. | serious | operator-question | done | `git diff --name-only main..HEAD` lists `tests/current_behavior.rs` and `tests/lsp_current_behavior.rs`; no `src/`, workflow, manifest, or lockfile changes in the feature diff. | CDC reconciled the final committed state at `b596714`; the working tree itself is clean for Slice02. |
| C3-2 | CLI current behavior is characterized for success and recoverable error paths. | `rg -n "current_behavior|characterization|missing|unsupported|semantic|write|success" tests src` and `cargo test --all-targets` | serious | RUST-001 | done | `tests/current_behavior.rs` covers missing input, unsupported extension, semantic failure, output write failure, and known-good verification; full `cargo test --all-targets` passed. | Error-path tests intentionally assert today's exit `0`. |
| C3-3 | Public HDDL constructor and transpiler domain/problem mismatch behavior is characterized. | `rg -n "from_hddl|domain.*problem|problem.*domain|catch_unwind|should_panic|current_behavior" tests src` and `cargo test --all-targets` | serious | RUST-002 | done | `tests/current_behavior.rs` adds four `catch_unwind` baselines for `HDDLProgram::from_hddl` and `Transpiler::from_hddl` domain/problem mismatches; full tests passed. | Test names mark the panics as current behavior. |
| C3-4 | Domain-only `remove-equality-constraints` behavior is characterized. | `rg -n "remove-equality|RemoveEqualityConstraints|catch_unwind|current_behavior|characterization" tests src` and `cargo test --all-targets` | serious | RUST-003 | done | `remove_equality_constraints_domain_only_current_behavior_panics` captures the current transform panic; full tests passed. | |
| C3-5 | LSP initialize metadata behavior is characterized or explicitly deferred with a harness limitation. | `rg -n "initialize|ServerInfo|CARGO_PKG_VERSION|0\\.1\\.0|characterization|deferred" tests src 01-cleanup/arc03-rust-best-practices/slice02-baseline-characterization-tests` | correctness | RUST-008 | done | `tests/lsp_current_behavior.rs` initializes the server over stdio and asserts the stale `serverInfo.version` value is `0.1.0`, distinct from `CARGO_PKG_VERSION`. | |
| C3-6 | LSP diagnostic failure-path behavior is characterized or explicitly deferred with a harness limitation. | `rg -n "did_save|diagnostic|non-file|missing|domain|unwrap|characterization|deferred" tests src 01-cleanup/arc03-rust-best-practices/slice02-baseline-characterization-tests` | correctness | RUST-005 | done | LSP tests characterize unsynced diagnostics as JSON-RPC `-32602` and problem-without-domain diagnostics as an empty full report. | Additional panic-oriented LSP paths are deferred until the repair slice can add deterministic hooks without exposing private internals. |
| C3-7 | The diagnostic `RwLock` contention candidate is handled as current behavior, not as a post-fix assertion. | `rg -n "RwLock|read guard|contention|diagnostic|current behavior|post-fix|deferred" tests src 01-cleanup/arc03-rust-best-practices/slice02-baseline-characterization-tests` | serious | RUST-004 | done | No post-fix contention assertion was added; the closing report records the deterministic re-entry condition. | Re-enter after the diagnostic path no longer holds the document read guard across awaited work or after a test hook exposes controlled awaited work. |
| C3-8 | RUST-006 dependency/lockfile policy is not mixed into this test baseline slice. | `git diff -- Cargo.toml .gitignore Cargo.lock` and inspect for absence of dependency-version/lockfile policy changes. | correctness | RUST-006 | done | `git diff -- Cargo.toml .gitignore Cargo.lock` produced no output. | No dev-dependencies were added. |
| C3-9 | No new ignored tests are introduced. | `git diff -- tests src Cargo.toml | rg "#\\[ignore\\]|ignore ="` | serious | ledger-discipline | done | `git diff -- tests src Cargo.toml | rg "#\\[ignore\\]|ignore ="` exited `1` with no output. | |
| C3-10 | The full local workflow-equivalent quality gate passes. | `cargo fmt --check`; `cargo check --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test --all-targets`; `cargo build --release --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check` | serious | arc01-arc02 | done | All listed commands passed on 2026-08-25. `cargo test --all-targets` included 13 new Slice02 tests. | |
| C3-11 | The audit runtime probes still show current behavior, proving no repair slipped into the baseline slice. | Run the four probes from `workbench/2026.08.25-audit-results-rust.md` and compare exit codes `0`, `0`, `101`, `101`. | serious | cdc-verification | done | Runtime probes returned `0`, `0`, `101`, `101` for missing input, unsupported extension, problem-as-domain panic, and domain-only remove-equality panic. | Later fix slices will intentionally change these baselines. |
| C3-12 | Slice02 closing report bubbles up the exact test baselines and any deferred LSP harness work before Slice03 opens. | Inspect `closing-report.md` for a row walk and Bubble-up to the arc section. | correctness | project-management | done | `closing-report.md` records the row walk, test inventory, LSP harness limits, runtime probes, and Slice03 bubble-up. | |

## What Worked

Two std-only integration test files were enough to cover the current CLI, public constructor/transpiler, transformation, and reachable LSP behavior without production edits or new dependencies. `env!("CARGO_BIN_EXE_*")` kept binary-path assertions aligned with Cargo's integration-test harness.

## Closure

Closed 2026-08-25 against the current `audit/rust-best-practices` feature worktree. The baseline remains intentionally pre-fix: CLI recoverable failures still exit `0`, library/transpiler mismatch paths still panic, and LSP diagnostics retain their current reachable behavior. LSP contention and additional panic-oriented protocol paths are explicitly deferred to repair slices where deterministic hooks or refactors can make those tests reliable without widening private APIs.
