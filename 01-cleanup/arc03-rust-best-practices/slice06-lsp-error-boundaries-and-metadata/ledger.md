# Slice06: LSP Error Boundaries And Metadata Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| C6-1 | The slice diff is limited to LSP error-boundary and metadata repair plus directly updated LSP tests, on top of the verified Slice05 base. | `git diff --name-only 6bd1b0a..HEAD` unless Slice05 has merged upstream, then inspect changed files. | serious | RUST-005/RUST-008 | done | Feature commit `fbb27d7` changes `src/language_server/request_handler.rs` and `tests/lsp_current_behavior.rs` relative to `6bd1b0a`. | Scope is limited to LSP request/error-boundary handling, metadata, and LSP tests. |
| C6-2 | `initialize` reports `CARGO_PKG_VERSION` instead of hard-coded `0.1.0`. | Updated `tests/lsp_current_behavior.rs` initialize test and `rg -n "CARGO_PKG_VERSION|0\\.1\\.0|ServerInfo" src tests`. | correctness | RUST-008 | done | Feature commit `fbb27d7`; CDC reproduced `lsp_initialize_reports_package_version` in `cargo test --test lsp_current_behavior` with 6 passed, 0 failed. `rg -n "CARGO_PKG_VERSION|0\\.1\\.0|ServerInfo" src tests/lsp_current_behavior.rs` finds `CARGO_PKG_VERSION` in `request_handler.rs` and the test, with no `0.1.0` matches. | |
| C6-3 | Unsynced diagnostics keep returning a structured JSON-RPC error without server panic. | `rg -n "unsynced|JSON-RPC|32602|invalid" tests/lsp_current_behavior.rs` and `cargo test --test lsp_current_behavior`. | correctness | RUST-005 | done | Feature commit `fbb27d7`; CDC reproduced `lsp_diagnostic_unsynced_document_returns_jsonrpc_error` in `cargo test --test lsp_current_behavior` with 6 passed, 0 failed; it asserts JSON-RPC code `-32602` and message `is not synced`. | |
| C6-4 | Non-file or malformed diagnostic URIs are handled without `unwrap` panic. | New or updated LSP harness test plus source inspection for URI conversion. | serious | RUST-005 | done | Feature commit `fbb27d7`; CDC reproduced `lsp_diagnostic_non_file_uri_returns_jsonrpc_error_without_panic` in `cargo test --test lsp_current_behavior` with 6 passed, 0 failed; source inspection shows `diagnostic_file_path` maps `to_file_path` failure to JSON-RPC `invalid_params`. | |
| C6-5 | `didSave` missing or unreadable file behavior is handled without server panic or is explicitly deferred with a re-entry condition. | LSP harness test where deterministic; otherwise closing-report deferral with source rationale. | serious | RUST-005 | done | Feature commit `fbb27d7`; CDC reproduced `lsp_did_save_missing_file_keeps_server_alive_and_document_unsynced` in `cargo test --test lsp_current_behavior` with 6 passed, 0 failed; source inspection shows read errors are logged and return without sync/panic. | Missing-file behavior is harness-tested; unreadable-file behavior follows the same `tokio::fs::read` error branch. |
| C6-6 | Missing or unreadable sibling-domain discovery is handled without server panic or is explicitly deferred with a re-entry condition. | LSP harness test where deterministic; otherwise closing-report deferral with source rationale. | serious | RUST-005 | done | Feature commit `fbb27d7`; CDC reproduced `lsp_diagnostic_missing_sibling_directory_returns_empty_report` in `cargo test --test lsp_current_behavior` with 6 passed, 0 failed; source inspection shows `read_dir`, `next_entry`, and sibling candidate read errors are logged and fall back/continue without `unwrap`. | Missing-directory behavior is harness-tested; unreadable sibling candidate handling is source-inspected because a portable permission-denied fixture is not deterministic across all runner contexts. |
| C6-7 | No-domain-found diagnostics are intentionally preserved or intentionally changed and tested. | Updated `tests/lsp_current_behavior.rs` no-domain test. | correctness | RUST-005 | done | Feature commit `fbb27d7`; CDC reproduced `lsp_diagnostic_problem_without_domain_returns_empty_report` in `cargo test --test lsp_current_behavior` with 6 passed, 0 failed; it preserves full report with zero items. | |
| C6-8 | RUST-004 lock-scope repair is not silently mixed into this slice unless explicitly promoted and recorded. | `git diff 6bd1b0a..HEAD -- src/language_server/request_handler.rs` and inspect lock scope. | correctness | slice03-fix-map | done | Feature commit `fbb27d7`; source inspection shows `diagnostic` still matches on `self.documents.read().await.get(&params.text_document.uri)` and still borrows `document` through awaited work. | RUST-004 remains for Slice07. |
| C6-9 | The full local workflow-equivalent gate passes. | `cargo fmt --check`; `cargo check --all-targets`; `RUSTFLAGS="-D rust-2024-compatibility" cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test --all-targets`; `cargo build --release --bins`; `./target/release/hddl_analyzer --help`; `actionlint .github/workflows/ci.yml`; `git diff --check` | serious | arc-plan | done | Feature commit `fbb27d7`; CDC reproduced all listed commands with exit `0` on 2026-08-26. Focused `cargo test --test lsp_current_behavior` also exited `0` with 6 passed, 0 failed. | `cargo test --all-targets` passed with existing ignored legacy tests unchanged. |
| C6-10 | Slice06 closing report walks every row and states all LSP edge-case deferrals, if any. | Inspect `closing-report.md` for C6-1 through C6-10 and `re-entry`. | correctness | ledger-discipline | done | Feature commit `fbb27d7`; `closing-report.md` records C6-1 through C6-10, final LSP behavior, and the remaining harness limitation/re-entry condition for a portable unreadable sibling-file fixture. | |

## What Worked

The existing stdio LSP harness was enough to exercise the user-visible
metadata and request-boundary failures without making private internals public.
Keeping RUST-004 separate made the diff easy to review: no document-map lock
ownership/clone/drop redesign is mixed into this slice.

## Closure

Closed by CC on 2026-08-26 as feature commit `fbb27d7` on
`fix/lsp-error-boundaries-and-metadata`. Final row count: C6-1 through C6-10
done, with no deferred or no-op Slice06 rows.
